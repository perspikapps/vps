# Exclude .agents/ from prettier formatting
# This is the canonical source for AI tooling; symlinked copies format via their own rules
.agents/

# Helm Go-template YAML isn't valid YAML - prettier's yaml parser chokes on
# the {{ }} directives (e.g. a bare `{{- if ... }}` before the first key).
charts/*/templates/

