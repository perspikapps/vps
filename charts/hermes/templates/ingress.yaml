{{/* @format */}}
{{- if .Values.dashboard.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
    name: {{ include "hermes.fullname" . }}
    labels:
        {{- include "hermes.labels" . | nindent 8 }}
    {{- if .Values.dashboard.ingress.tlsIssuer }}
    annotations:
        cert-manager.io/cluster-issuer: {{ .Values.dashboard.ingress.tlsIssuer }}
    {{- end }}
spec:
    ingressClassName: {{ .Values.dashboard.ingress.className }}
    rules:
        - host: {{ .Values.dashboard.ingress.host }}
          http:
              paths:
                  - path: /
                    pathType: Prefix
                    backend:
                        service:
                            name: {{ include "hermes.fullname" . }}
                            port:
                                name: dashboard
    {{- if .Values.dashboard.ingress.tlsIssuer }}
    tls:
        - hosts:
              - {{ .Values.dashboard.ingress.host }}
          secretName: {{ include "hermes.fullname" . }}-tls
    {{- end }}
{{- end }}
